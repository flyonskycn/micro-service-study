spring:
  zipkin:    base-url: http://localhost:9411/

server:
  port: 8001

eureka:
  instance: 
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/